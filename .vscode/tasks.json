{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "flash with bootloader",
            "type": "shell",
            "command": "\"C:\\work\\V003\\projects\\ch32v0-uprog.exe\"  --baud 460800 --device COM18 .\\build\\firmware.elf.bin"
        },
        {
            "label": "cmake build",
            "type": "shell",
            "command": "cmake --build ${command:cmake.buildDirectory} --config Debug --target all",
            "problemMatcher": "$gcc"
        },
        {
            "label": "update fw",
            "dependsOrder": "sequence",
            "dependsOn": [
                "cmake build",
                "flash with bootloader"
            ]
        }
    ]
}